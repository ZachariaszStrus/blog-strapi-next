query searchArticles($searchedWord: String!) {
    search(query: $searchedWord){
        articles {
            data {
                id
                attributes {
                    title
                    slug
                    description
                }
            }
        }
    }
}